services:
  server:
    container_name: tg_bot
    restart: unless-stopped
    environment:
      - TOKEN
      - CS_IP
    build:
      context: .
    ports:
      - 8080:8080
    volumes:
      - ./data:/data
networks:
  default:
    external: true
    name: find-context